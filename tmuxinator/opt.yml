# ~/.tmuxinator/.yml
# you can make as many tabs as you wish...

name: opt
root: /opt
windows:
  - opt:
      layout: main-vertical
      panes:
        - 
        - #empty, will just run plain bash
        - top
